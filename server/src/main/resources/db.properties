# EthioRide Server — Database Configuration
# Update with your MySQL credentials before running

db.host=localhost
db.port=3306
db.name=ethioride
db.user=root
db.password=your_password_here
db.pool.size=10
db.connection.timeout=30000
