# EthioRide — Database Configuration
# Fill in your MySQL credentials before running the server

db.host=localhost
db.port=3306
db.name=ethioride
db.user=root
db.password=user_ur_db_password
db.pool.size=10
db.connection.timeout=30000
